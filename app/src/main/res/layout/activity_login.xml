<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.yamil.compraeconomica.LoginActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:orientation="horizontal"
        android:background="@color/colorBlancoAlmendra">
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            />
    </LinearLayout>

    <EditText
        android:id="@+id/edit_email"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="250dp"
        android:background="@drawable/edittext_esquina_redondeada"/>

    <EditText
        android:id="@+id/edit_password"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:hint="Contraseña"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/edit_email"
        android:layout_marginTop="10dp"
        android:inputType="textPassword"
        android:background="@drawable/edittext_esquina_redondeada"/>
    <Button
        android:id="@+id/btn_login"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/edit_password"
        android:layout_marginTop="10dp"
        android:text="Login"
        android:textColor="#FFF8F8FA"
        android:background="@color/colorBlancoAlmendraOscuro"/>
    <TextView
        android:id="@+id/recuperar_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Has olvidado tu contraseña?"
        android:layout_below="@id/btn_login"
        android:layout_marginTop="10dp"
        android:layout_centerHorizontal="true"
        android:textColor="@color/colorPrimary"
        />
</RelativeLayout>
